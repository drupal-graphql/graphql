parameters:
  graphql.type_map:
    String:
      - text
      - varchar
      - varchar_ascii
      - blob
    Int:
      - int
    Float:
      - numeric
      - float

services:
  graphql.type_mapper:
    class: Drupal\graphql_content\TypeMapper
    arguments: ['%graphql.type_map%']
