parameters:
  graphql.type_map:
    String:
      - text
      - varchar
      - varchar_ascii
      - blob
    Int:
      - int
    Float:
      - numeric
      - float

services:
  graphql.type_matcher:
    class: Drupal\graphql_content\TypeMatcher
    arguments: ['%graphql.type_map%']
