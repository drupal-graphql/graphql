query ($path: String!) {
  route:route(path: $path) {
    node:entity {
      file {
        entityUrl {
          internalPath:path
          routed
        }
        fileSize
        mimeType
      }
    }
  }
}
