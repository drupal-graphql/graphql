services:
  graphql_twig.twig.inject:
    class: Drupal\graphql_twig\GraphQLTwigExtension
    arguments: ['@renderer', '@url_generator', '@theme.manager', '@date.formatter']
    tags:
      - { name: twig.extension }